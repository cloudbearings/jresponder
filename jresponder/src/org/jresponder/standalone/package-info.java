/**
 * Support classes for standalone operation.  Most of them do something
 * special in "standalone" mode but fall back to default Spring behavior if
 * run inside a regular web container ("webapp" mode).
 */
package org.jresponder.standalone;