/**
 * Spring MVC API calls.  Some are for public (so you can wire up
 * subscribing from your site) and some are for admin (used by the admin
 * pages).
 * <p>
 * Calls are either JSON-RPC 2.0, or some are REST-like, depending on whic
 * of those models fits the call better.
 */
package org.jresponder.webapi;